
/*<script>
  function updateItemValue() {
  var itemSelect = document.querySelector('select[name="item"]');
  var hiddenItemField = document.querySelector('input[name="item_number"]');
  var productMapping = {
    "Men Regular Fit Sweatshirt": 1234,
  "COIMBATORE": 3456
  };

  var selectedItem = itemSelect.value;
  hiddenItemField.value = productMapping[selectedItem] || '';

  console.log("Selected Item: " + selectedItem);
  console.log("Item Number: " + hiddenItemField.value);
}
  window.onload = function () {
  var itemSelect = document.querySelector('select[name="item"]');
  itemSelect.addEventListener('change', updateItemValue);
  updateItemValue();
};
</script> */